name: backend

on:
  push:
    branches: ["**"]
    paths:
      - "jcn-freight/backend/**"
      - ".github/workflows/backend.yml"
  pull_request:
    paths:
      - "jcn-freight/backend/**"
      - ".github/workflows/backend.yml"

jobs:
  testes:
    name: pytest
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: jcn-freight/backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: jcn-freight/backend/requirements-dev.txt

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Rodar a suíte
        run: pytest
